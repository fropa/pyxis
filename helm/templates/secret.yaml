apiVersion: v1
kind: Secret
metadata:
  name: infrawatch-secret
  namespace: {{ .Values.namespace }}
  labels:
    app: {{ include "infrawatch-agent.name" . }}
type: Opaque
stringData:
  api-key: {{ .Values.agent.apiKey | quote }}
