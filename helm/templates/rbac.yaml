{{- if .Values.rbac.create }}
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "pyxis-agent.serviceAccountName" . }}
  namespace: {{ .Values.namespace }}
  labels:
    app: {{ include "pyxis-agent.name" . }}
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: {{ include "pyxis-agent.name" . }}
  labels:
    app: {{ include "pyxis-agent.name" . }}
rules:
  - apiGroups: [""]
    resources: ["events", "nodes", "pods", "namespaces", "services"]
    verbs: ["get", "list", "watch"]
  - apiGroups: ["apps"]
    resources: ["deployments", "daemonsets", "replicasets", "statefulsets"]
    verbs: ["get", "list", "watch"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: {{ include "pyxis-agent.name" . }}
  labels:
    app: {{ include "pyxis-agent.name" . }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: {{ include "pyxis-agent.name" . }}
subjects:
  - kind: ServiceAccount
    name: {{ include "pyxis-agent.serviceAccountName" . }}
    namespace: {{ .Values.namespace }}
{{- end }}
