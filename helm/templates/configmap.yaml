apiVersion: v1
kind: ConfigMap
metadata:
  name: pyxis-shipper
  namespace: {{ .Values.namespace }}
  labels:
    app: {{ include "pyxis-agent.name" . }}
data:
  shipper.py: |
    {{ .Files.Get "files/shipper.py" | nindent 4 }}
